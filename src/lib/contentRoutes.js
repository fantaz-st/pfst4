import { createContentRoute } from "./contentRoute";

export const hrContentRoute = createContentRoute({ language: "hr" });
export const enContentRoute = createContentRoute({ language: "en", prefix: "en" });
